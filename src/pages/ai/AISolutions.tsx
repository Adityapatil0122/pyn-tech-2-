
import React from "react";
import { Navigate } from "react-router-dom";

const AISolutions = () => {
  // Redirect to homepage as this page is no longer needed
  return <Navigate to="/" />;
};

export default AISolutions;
