import PynCTA from "@/components/shared/PynCTA";

const CTASection = () => {
  return (
    <PynCTA
      eyebrow="Start with one useful conversation"
      title="Tell us where your workflow, product, or marketing engine needs to be sharper."
      description="We will map the right first move, whether that is a landing page, app, chatbot, automation, campaign, or full digital system."
      primaryText="Book a Strategy Call"
      secondaryText="Send Project Details"
    />
  );
};

export default CTASection;
